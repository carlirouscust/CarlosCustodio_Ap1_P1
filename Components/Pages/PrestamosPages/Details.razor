@page "/Prestamos/Details/{prestamoId:int}"
@rendermode InteractiveServer
@inject NavigationManager navigation
@inject PrestamosService prestamosSevice

<PageTitle>Detalles de prestamo</PageTitle>

<div class="container">
    <div class="card shadow-lg rounded">
        <div class="card bg-primary text-white py-2 rounded">
            <h5 class="text-center">Detalles de prestamo</h5>
        </div>
        <div class="card-body text-center">          
        </div>
        <div class="container">
            <div class="row">
                <div class="col md-6">
                    <div class="card-body">                        
                        <ul class="list-unstyled">                         
                            <li class="text-center">Deudor:<strong>@prestamo.deudores.Nombres</strong></li>
                            <li class="text-center">Concepto:<strong>@prestamo.concepto</strong></li>
                            <li class="text-center">Monto:<strong>@prestamo.monto</strong></li>
                        </ul>

                    </div>
                    <div class="card-body text-center">                       
                        <a href="/Prestamos/Index" class="btn btn-primary"><span class="bi bi-arrow-left mt-3"></span>Volver</a>
                    </div>
                </div>

            </div>

        </div>

    </div>
 </div>
@code {
        [Parameter]
        public int PrestamoId { get; set; }
        public Prestamos? prestamo { get; set; } = new Prestamos();
        private bool Validacion;

        protected override async Task OnInitializedAsync()
        {
            if (prestamo != null)
            {
                prestamo = await prestamosSevice.Buscar(PrestamoId);
            }
        }
}
